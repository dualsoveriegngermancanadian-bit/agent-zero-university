# Optional release identifier shown by the health endpoint.
APP_VERSION=1.0.0

# Comma-separated trusted browser origins. Leave blank when no browser client is attached.
# CORS_ALLOWED_ORIGINS=https://app.example.com

# Hardware-token access remains disabled until an approved identity provider is integrated.
